name: Docker Image CI PULL REQUEST

on:
  pull_request:
    branches: [ master ]

jobs:

  build-javascript:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Say hello
      run: echo "👋 Hello World 🌍"
