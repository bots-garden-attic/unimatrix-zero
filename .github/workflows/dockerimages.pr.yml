name: Greetings PULL REQUEST

on:
  pull_request:
    branches: [ master ]

jobs:

  Greetings:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Say hello
      run: echo "👋 Hello World 🌍"
